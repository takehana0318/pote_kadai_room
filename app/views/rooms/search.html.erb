<h1>検索結果</h1>

<p>合計件数: <%= @total_count %>件</p>

<% if @rooms.present? %>
  <table>
    <thead>
      <tr>
        <th>施設名</th>
        <th>住所</th>
        <th>施設詳細</th>
        <th>宿泊料金（/日）</th>
      </tr>
    </thead>
    <tbody>
      <% @rooms.each do |room| %>
        <tr>
          <td><%= link_to room.name, room_path(room) %></td>
          <td><%= room.address %></td>
          <td><%= room.description %></td>
          <td><%= number_to_currency(room.price_per_day) %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% else %>
  <p>検索結果は見つかりませんでした。</p>
<% end %>

<%= link_to '戻る', rooms_path, class: 'btn btn-secondary' %>
